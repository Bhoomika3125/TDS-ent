name: My First Action

on: [workflow_dispatch]  # Manual trigger button

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: 25ds3000152@ds.study.iitm.ac.in  # Your required step
        run: echo "Hello from my GitHub Action!"
      
      - name: List files
        run: ls -la
